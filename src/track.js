class Track {
  constructor(props) {
    this.title = props.title;
    this.artist = props.artist;
    this.length = props.length;
    this.isExplicit = props.isExplicit;
  }
}

module.exports = Track;
